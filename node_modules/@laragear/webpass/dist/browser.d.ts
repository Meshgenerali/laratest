/**
 * Check if the browser supports WebAuthn
 *
 * @return {boolean}
 */
export declare function isSupported(): Promise<boolean>;
/**
 * Check if the browser doesn't support WebAuthn
 */
export declare function isNotSupported(): Promise<boolean>;
/**
 * Check if the browser doesn't support WebAuthn
 */
export declare function isUnsupported(): Promise<boolean>;
/**
 * Check if the browser can immediately authenticate without picking credentials.
 */
export declare function isAutomatic(): Promise<boolean>;
/**
 * Check if the browser cannot immediately authenticate without picking credentials.
 */
export declare function isNotAutomatic(): Promise<boolean>;
/**
 * Check if the browser cannot immediately authenticate without picking credentials.
 */
export declare function isManual(): Promise<boolean>;
